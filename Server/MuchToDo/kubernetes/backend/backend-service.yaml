apiVersion: v1
kind: Service
metadata:
  name: muchtodo-backend-service
  namespace: muchtodo
spec:
  selector:
    app: muchtodo-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
